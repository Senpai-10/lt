# ./.tmuxinator.yml

name: lt
root: .

windows:
    - nvim:
          - clear
          - nvim .
    - run:
          - yarn tauri dev
    - bacon:
          - cd src-tauri/
          - bacon -j clippy
    - git:
          - clear
          - gs
